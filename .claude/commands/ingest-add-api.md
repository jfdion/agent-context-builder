Add a new source subdirectory to an existing knowledge base using the Python/API backend.

Usage: uv run ingest-add <source> <destination> [--rpm INT] [--max-binary-mb INT]

!uv run ingest-add $ARGUMENTS
