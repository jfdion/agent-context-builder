Re-process an amended source directory in an existing knowledge base using the Python/API backend.

Usage: uv run ingest-amend <source> <destination> [--rpm INT] [--max-binary-mb INT]

!uv run ingest-amend $ARGUMENTS
